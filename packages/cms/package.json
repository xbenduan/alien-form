{
  "name": "@alien-form/cms",
  "version": "0.3.0",
  "description": "Minimal CMS core: async API functions + projection utilities. Zero state, zero framework dependency.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudbase/js-sdk": "^3.0.0",
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
