{
  "$schema": "https://developers.cloudflare.com/pages/configuration/deploy-button-schema.json",
  "deploy": {
    "projectName": "alien-mdm",
    "buildCommand": "pnpm run build",
    "buildOutputDirectory": "apps/alien-mdm-v1/dist",
    "rootDirectory": "",
    "environmentVariables": {
      "NODE_VERSION": "20"
    }
  }
}
