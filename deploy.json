{
  "$schema": "https://developers.cloudflare.com/pages/configuration/deploy-button-schema.json",
  "deploy": {
    "projectName": "alien-cms",
    "buildCommand": "pnpm run build",
    "buildOutputDirectory": "apps/alien-cms/dist",
    "rootDirectory": "",
    "environmentVariables": {
      "NODE_VERSION": "20"
    }
  }
}
