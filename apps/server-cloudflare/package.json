{
  "name": "@alien-form/server-cloudflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute alien-cms --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute alien-cms --remote --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
