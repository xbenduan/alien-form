# Reaction 测试

演示 `x-reaction` 系统的各种依赖模式，包括匹配表达式、计算处理器和数组项联动。

## Case 1: 匹配 + 依赖 → 显示控制

一个下拉框控制目标字段的可见、隐藏或移除。

```tsx file="../_demos/reaction-case1.tsx" preview
```

## Case 2: 表达式 + 依赖 → 值计算

两个输入框拼接为全名。

```tsx file="../_demos/reaction-case2.tsx" preview
```

## Case 3: 计算处理器 + 依赖 → 禁用

角色选择通过命名处理器控制另一个字段是否可编辑。

```tsx file="../_demos/reaction-case3.tsx" preview
```

## Case 4: 多依赖 → 必填

两个开关都为"是"时字段才变为必填。

```tsx file="../_demos/reaction-case4.tsx" preview
```

## Case 5: 匹配 + 依赖 → 数据源

国家选择动态变更城市下拉选项。

```tsx file="../_demos/reaction-case5.tsx" preview
```

## Case 6: 表达式 → 动态标题

数量选择器动态更新另一字段的标题。

```tsx file="../_demos/reaction-case6.tsx" preview
```

## Case 7: 数组项内的 x-reaction

添加行后观察数组项内的 x-reaction 是否正常工作。

```tsx file="../_demos/reaction-case7.tsx" preview
```
