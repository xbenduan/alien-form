# Alien MDM 前端环境变量示例。
#
# 复制为 .env.local 后按需修改。核心开关是 VITE_API_BASE：它决定前端把
# schema.* / records.* / auth.* 打到哪个后端。两套后端接口完全同构，切换只改这里。
#
# 1) 请求本地 Node 后端（apps/alien-server，默认）：
#    dev 期 vite 已把 /api 代理到 http://localhost:8787，保持默认即可，无需设置。
# VITE_API_BASE=/api
#
# 2) 请求 Cloudflare Worker 后端（apps/alien-worker + D1）：
#    - 同源一体化部署（推荐）：SPA 与 /api 由同一个 Worker 托管，用相对路径即可。
# VITE_API_BASE=/api
#    - 本地起 wrangler dev（默认 8787）时，同样走 vite 代理或直接指向：
# VITE_API_BASE=http://localhost:8787/api
#    - 指向已部署的独立 Worker 域名：
# VITE_API_BASE=https://alien-mdm.<your-account>.workers.dev/api
