{
  "name": "formily-bao",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @formily-bao/demo run dev",
    "dev:docs": "pnpm --filter @formily-bao/docs run dev",
    "build": "pnpm -r run build",
    "package:source": "bash ./scripts/package-project.sh",
    "test": "pnpm -r run test",
    "test:core": "pnpm --filter @formily-bao/core run test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
